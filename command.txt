docker -v  // เช็ค version
docker ps --all  // เช็คว่ามี container อะไรในเครื่องเราบ้าง
docker run hello-world  //
// CI/CD  
cls  // clear screen
docker run busybox echo 'Hello World in docker'  //
docker run busybox ls  //
docker run busybox ping google.com  // รันค้าง
docker exec -it 28351dadce9f sh //เข้าใน container
docker exec -it 28351dadce9f bash  //
docker run nginx  //
docker exec -it (conId) sh  // run ของ nginx
docker stop (conId)
docker remove (conId)
docker run -p 80:80 nginx  //
docker run -p 9000:80 nginx  //
docker run -d -p 8000:80 nginx  //